apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nginx

resources:
- namespace.yaml
- deployment.yaml
- service.yaml

labels:
- includeSelectors: true
  pairs:
    liatr: io

images:
- name: nginx
  newTag: 1.29.2

